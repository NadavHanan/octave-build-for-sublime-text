# octave-build-for-sublime-text
if you are having problems with the plot function this may help, I know its weird but it works (for me).
save both files to: `\AppData\Roaming\Sublime Text\Packages\User`.
